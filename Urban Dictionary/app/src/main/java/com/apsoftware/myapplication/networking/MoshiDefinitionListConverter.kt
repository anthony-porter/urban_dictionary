package com.apsoftware.myapplication.networking

import com.squareup.moshi.FromJson
import com.squareup.moshi.Moshi
import com.squareup.moshi.Types

class MoshiDefinitionListConverter {

    /*@FromJson
    val adapterType = Types.newParameterizedType(List<*>::class.java, returnType)
    val adapter = Moshi.Builder().build().adapter<List<T>>(adapterType)

    val returnedObjects = adapter.fromJson(responseStr)*/
}